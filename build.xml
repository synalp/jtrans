
<project name="jtrans" default="compile" basedir=".">
    <property name="src" location="src"/>
    <property name="build" location="bin"/>
    <property name="dist"  location="dist"/>
    <property name="libs"  location="libs"/>
    <property name="version" value="1.2"/>

	<path id="my.classpath">
		<pathelement location="${libs}/*.jar"/>
	</path>
	
    <target name="compile" description="compile the source " >
        <echo message="JVM - ${ant.java.version}" />
        <mkdir dir="${build}"/>
        <javac debug="true" srcdir="${src}" destdir="${build}" encoding="UTF-8">
    	    <classpath refid="my.classpath" />
        </javac>
    </target>

    <target name="jar" depends="compile" description="generate the distribution" >
    	<mkdir dir="${dist}"/>
	<manifest file="${dist}/MANIFEST.MF">
		<attribute name="Class-Path" value="libs/*.jar"/>
		<attribute name="Main-Class" value="plugins.applis.SimpleAligneur.Aligneur"/>
		<attribute name="Implementation-Title" value="JTrans"/>
		<attribute name="Implementation-Version" value="${version}"/>
		<attribute name="Implementation-Vendor" value="Christophe Cerisara"/>
		<attribute name="Build-Date" value="${TODAY}"/>
	</manifest>
        <jar jarfile="${dist}/jtrans.jar" basedir="${build}"
             manifest="${dist}/MANIFEST.MF">
        </jar>
    </target>

    <target name="clean"
        description="clean up" >
        <delete dir="${build}"/>
        <delete dir="${dist}"/>
    </target>

</project>
